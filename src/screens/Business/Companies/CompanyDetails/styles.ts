import styled from 'styled-components/native'

export const CompanyDetailsContainer = styled.View`
  padding: ${({ theme }) => theme.space.m};
`

export const CompanyDetailsFooter = styled.View`
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
`
